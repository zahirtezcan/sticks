
centralize sift-down operation for heap related algorithms (functions in heap.h and sort.h)
stable sort
nlogn merge-inplace 

type traits / tag dispatching etc (i don't think it is worth doing, libcxx would just be fine:)
